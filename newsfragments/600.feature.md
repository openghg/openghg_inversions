Add a nested-domain RHIME recipe that combines native outer and 6 km inner grids without double-counting their overlapping emissions, and allows a coarser emissions domain to be interpolated explicitly onto the inner footprint grid.
