Add a ``merge-paris-outputs`` CLI command that safely merges sequential legacy or latest PARIS flux and concentration files.

Allow nested RHIME runs to select a coarser emissions domain and explicitly interpolate its flux density onto the inner footprint grid.
