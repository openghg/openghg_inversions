Modern concentration, flux, and country-total outputs now use every sampled MCMC chain. The legacy ``run_hbmcmc.py`` entry point continues to use chain 0 by default for derived outputs, warns about that compatibility behavior, and provides ``--all-chains`` as the recommended opt-in.
