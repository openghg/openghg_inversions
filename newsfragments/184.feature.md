Added directional, source-preserving `state_to_native` and `state_to_flux` reconstruction for retained basis states. Deprecated `interpolate` with its historical multisource total/eager behavior intact; the native-map expansion adapter is now internal. Total-grid postprocessing sums sources explicitly and preserves existing flux time labels.
