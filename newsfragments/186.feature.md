Add independent channel-labelled boundary scaling, state activity, and optional offsets to the linked CO2/O2 prepared-input runner, with reconstructable component predictions and matching configuration options.
