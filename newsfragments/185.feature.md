Add separate CO2 and O2 PARIS concentration products from one linked posterior, with component closure, provenance, and optional supported native flux products.
Reject ambiguous source spellings such as `GPP` and `gpp` during linked input preparation, while allowing repeated states with the same source label.
